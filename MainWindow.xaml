<Window x:Class="DropTableEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Drop Table Editor" Height="300" Width="600" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Window.Background>
        <LinearGradientBrush EndPoint="0,1">
            <GradientStop Offset="0.0" Color="#aebad1" />
            <GradientStop Offset="1.0" Color="#cdd4df" />
        </LinearGradientBrush>
    </Window.Background>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="ROSE Online - Drop Table Editor" FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#2d3e50"/>

        <!-- Data Path Selection -->
        <GroupBox Grid.Row="1" Header="3DDATA Folder Path" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="DataPathTextBox" Grid.Column="0" Height="25" VerticalContentAlignment="Center" Margin="0,0,10,0" KeyDown="DataPathTextBox_KeyDown"/>
                <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse..." Width="100" Height="25" Click="Browse_Click"/>
            </Grid>
        </GroupBox>

        <!-- Status -->
        <TextBlock Grid.Row="2" x:Name="StatusText" Text="Please select your ROSE Online 3DDATA folder." 
                   Foreground="Gray" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <!-- Spacer -->
        <StackPanel Grid.Row="3"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="LoadButton" Content="Open Drop Table Editor" Width="180" Height="35" 
                    Click="Load_Click" IsEnabled="False" Margin="0,0,10,0" FontSize="14"/>
            <Button Content="Exit" Width="100" Height="35" Click="Exit_Click" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
